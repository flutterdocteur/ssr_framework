name: ssr_server
description: Server-side rendering engine for SSR Framework
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.2

dependencies:
  ssr_core:
    path: ../ssr_core
  alfred: ^1.1.3
  jinja: ^0.6.7
  path: ^1.9.0
  sqlite3: ^2.4.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
